# Existing AI documentation assistant
LLM_GATEWAY_API_KEY=
LLM_GATEWAY_MODEL=anthropic/claude-3.5-sonnet

# Newsletter option 1: Buttondown
# Create an API key with subscriber write permission and keep it server-side.
BUTTONDOWN_API_KEY=

# Newsletter option 2: provider-neutral webhook
# Used only when BUTTONDOWN_API_KEY is not configured.
NEWSLETTER_WEBHOOK_URL=
NEWSLETTER_WEBHOOK_TOKEN=
